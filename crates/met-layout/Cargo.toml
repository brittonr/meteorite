[package]
name = "met-layout"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Layout primitives for Dioxus: stack, grid, container, sidebar, split pane"

[dependencies]
dioxus = { workspace = true }
met-core = { workspace = true }
