[package]
name = "met-table"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Data table component for Dioxus with sorting, filtering, and pagination"

[dependencies]
dioxus = { workspace = true }
met-core = { workspace = true }
